import { centralColor } from '../../../CentralCSS/Color/Color';

export const styles = {
  userLoginMenuContainer: {
    '& ul': {
      color: centralColor.header.titleBar.userLoginMenuTextcolor,
      backgroundColor: centralColor.header.titleBar.userLoginMenuBackgroundcolor
    }
  }
};