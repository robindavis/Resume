import { colorTheme } from './ColorThemeCenter/ColorThemeCenter';
import { centralDimension } from './CentralCSS/Dimension/Dimension';

export const styles = {
  'resumeContainer': {
    color: colorTheme.mainApp.textColor,
    backgroundColor: colorTheme.mainApp.backgroundColor,
    height: centralDimension.mainApp.height
  }
};